service: {{SERVICE_NAME}}

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs12.x

functions:
  graphql:
    handler: ./dist/index.handler
    events:
    - http:
        method: any
        path: /{proxy+}